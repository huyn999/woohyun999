name: fp_x_s_hugetlb
# AUTO-GENERATED failprobe workload — v2 extension probe: hugepage 매핑 (MAP_HUGETLB — 미디어/DB 대형버퍼)
# features: hugetlb
# expected risks: hugetlb:HIGH
params:
  bytes: {default: 52428800}
  port:  {default: 18080}
  phase_gap_ms: {default: 400}   # phase별 정지 창(호환성 스윕용); 시간 측정 시 0
phases: [init, base_mem, f_hugetlb, ready, served_first, steady]
metrics: []
resident:
  from_param: bytes
  bytes_per_unit: 1
  overhead_mib: 12
