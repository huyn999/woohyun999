name: fp_s_eventfd
# AUTO-GENERATED failprobe workload — single-feature probe: eventfd (이벤트 루프 wakeup)
# features: eventfd
# expected risks: eventfd:low
params:
  bytes: {default: 52428800}
  port:  {default: 18080}
  phase_gap_ms: {default: 400}   # phase별 정지 창(호환성 스윕용); 시간 측정 시 0
phases: [init, base_mem, f_eventfd, ready, served_first, steady]
metrics: []
resident:
  from_param: bytes
  bytes_per_unit: 1
  overhead_mib: 8
