name: fp_p21_netlink__large_heap
# AUTO-GENERATED failprobe workload — pair probe: NETLINK_ROUTE 소켓 (네트워크 상태 감시) + 대형 힙 (브라우저/게임의 리소스 로딩)
# features: netlink,large_heap
# expected risks: netlink:HIGH — netlink 복원 제한; large_heap:none — dump 이미지만 커짐
params:
  bytes: {default: 52428800}
  port:  {default: 18080}
  phase_gap_ms: {default: 400}   # phase별 정지 창(호환성 스윕용); 시간 측정 시 0
phases: [init, base_mem, f_netlink_l01, f_netlink_l02, f_netlink_l03, f_netlink_l04, f_netlink_l05, f_netlink, f_large_heap_l01, f_large_heap_l02, f_large_heap, ready, served_first, steady]
metrics: []
resident:
  from_param: bytes
  bytes_per_unit: 1
  overhead_mib: 308
