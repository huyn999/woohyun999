name: fp_p09_lock_flock__large_heap
# AUTO-GENERATED failprobe workload — pair probe: flock 배타 락 보유 (단일 인스턴스 락파일) + 대형 힙 (브라우저/게임의 리소스 로딩)
# features: lock_flock,large_heap
# expected risks: lock_flock:HIGH — 기본 옵션 dump 거부 예상 (--file-locks 필요); large_heap:none — dump 이미지만 커짐
params:
  bytes: {default: 52428800}
  port:  {default: 18080}
  phase_gap_ms: {default: 400}   # phase별 정지 창(호환성 스윕용); 시간 측정 시 0
phases: [init, base_mem, f_lock_flock_l01, f_lock_flock_l02, f_lock_flock, f_large_heap_l01, f_large_heap_l02, f_large_heap, ready, served_first, steady]
metrics: []
resident:
  from_param: bytes
  bytes_per_unit: 1
  overhead_mib: 308
