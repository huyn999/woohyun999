name: fp_w_sam_bad_order
# AUTO-GENERATED failprobe workload — [R] webOS-faithful: SAM 순서 실수 재현 — 트리 생성 후 setsid (fp_w_sam과 한 성분 대조쌍, 클러스터 ② 재현)
# features: child,grandchild,session,unix_listen
# expected risks: child:low — 프로세스 트리 dump; grandchild:medium — 깊은 트리; session:medium — 세션 리더 복원; unix_listen:medium — bind 경로 재생성
params:
  bytes: {default: 52428800}
  port:  {default: 18080}
  phase_gap_ms: {default: 400}   # phase별 정지 창(호환성 스윕용); 시간 측정 시 0
phases: [init, base_mem, f_child, f_grandchild, f_session, f_unix_listen_l01, f_unix_listen_l02, f_unix_listen_l03, f_unix_listen_l04, f_unix_listen_l05, f_unix_listen_l06, f_unix_listen, ready, served_first, steady]
metrics: []
resident:
  from_param: bytes
  bytes_per_unit: 1
  overhead_mib: 8
