name: fp_p13_lock_posix__epoll
# AUTO-GENERATED failprobe workload — pair probe: fcntl POSIX 레코드 락 보유 (DB 파일 락) + epoll 인스턴스 + 등록 fd (이벤트 루프)
# features: lock_posix,epoll
# expected risks: lock_posix:HIGH — --file-locks 필요; epoll:low
params:
  bytes: {default: 52428800}
  port:  {default: 18080}
  phase_gap_ms: {default: 400}   # phase별 정지 창(호환성 스윕용); 시간 측정 시 0
phases: [init, base_mem, f_lock_posix_l01, f_lock_posix_l02, f_lock_posix_l03, f_lock_posix, f_epoll_l01, f_epoll_l02, f_epoll_l03, f_epoll_l04, f_epoll, ready, served_first, steady]
metrics: []
resident:
  from_param: bytes
  bytes_per_unit: 1
  overhead_mib: 8
