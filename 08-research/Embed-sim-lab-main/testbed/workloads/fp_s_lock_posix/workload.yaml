name: fp_s_lock_posix
# AUTO-GENERATED failprobe workload — single-feature probe: fcntl POSIX 레코드 락 보유 (DB 파일 락)
# features: lock_posix
# expected risks: lock_posix:HIGH — --file-locks 필요
params:
  bytes: {default: 52428800}
  port:  {default: 18080}
  phase_gap_ms: {default: 400}   # phase별 정지 창(호환성 스윕용); 시간 측정 시 0
phases: [init, base_mem, f_lock_posix_l01, f_lock_posix_l02, f_lock_posix_l03, f_lock_posix, ready, served_first, steady]
metrics: []
resident:
  from_param: bytes
  bytes_per_unit: 1
  overhead_mib: 8
