name: fp_s_session
# AUTO-GENERATED failprobe workload — single-feature probe: setsid로 세션 분리 (데몬화 관례)
# features: session
# expected risks: session:medium — 세션 리더 복원
params:
  bytes: {default: 52428800}
  port:  {default: 18080}
  phase_gap_ms: {default: 400}   # phase별 정지 창(호환성 스윕용); 시간 측정 시 0
phases: [init, base_mem, f_session, ready, served_first, steady]
metrics: []
resident:
  from_param: bytes
  bytes_per_unit: 1
  overhead_mib: 8
