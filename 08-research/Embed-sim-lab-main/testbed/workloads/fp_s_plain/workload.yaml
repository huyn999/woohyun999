name: fp_s_plain
# AUTO-GENERATED failprobe workload — single-feature probe: 아무 자원도 없는 최소 앱 (대조군)
# features: plain
# expected risks: plain:none
params:
  bytes: {default: 52428800}
  port:  {default: 18080}
  phase_gap_ms: {default: 400}   # phase별 정지 창(호환성 스윕용); 시간 측정 시 0
phases: [init, base_mem, f_plain, ready, served_first, steady]
metrics: []
resident:
  from_param: bytes
  bytes_per_unit: 1
  overhead_mib: 8
