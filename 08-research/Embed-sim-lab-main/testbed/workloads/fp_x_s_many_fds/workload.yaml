name: fp_x_s_many_fds
# AUTO-GENERATED failprobe workload — v2 extension probe: fd 256개 (대규모 fd 테이블 — 스케일 축)
# features: many_fds
# expected risks: many_fds:low — 규모만
params:
  bytes: {default: 52428800}
  port:  {default: 18080}
  phase_gap_ms: {default: 400}   # phase별 정지 창(호환성 스윕용); 시간 측정 시 0
phases: [init, base_mem, f_many_fds, ready, served_first, steady]
metrics: []
resident:
  from_param: bytes
  bytes_per_unit: 1
  overhead_mib: 10
