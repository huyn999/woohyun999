name: fp_s_mlock
# AUTO-GENERATED failprobe workload — single-feature probe: mlock된 영역 (오디오 언더런 방지 버퍼)
# features: mlock
# expected risks: mlock:medium — 권한/한도
params:
  bytes: {default: 52428800}
  port:  {default: 18080}
  phase_gap_ms: {default: 400}   # phase별 정지 창(호환성 스윕용); 시간 측정 시 0
phases: [init, base_mem, f_mlock_l01, f_mlock_l02, f_mlock_l03, f_mlock_l04, f_mlock, ready, served_first, steady]
metrics: []
resident:
  from_param: bytes
  bytes_per_unit: 1
  overhead_mib: 9
