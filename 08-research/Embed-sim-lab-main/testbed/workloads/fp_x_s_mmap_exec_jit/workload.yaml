name: fp_x_s_mmap_exec_jit
# AUTO-GENERATED failprobe workload — v2 extension probe: 실행가능 익명 페이지 + 실제 점프 (JIT 최소형 — V8/JS엔진)
# features: mmap_exec_jit
# expected risks: mmap_exec_jit:HIGH — exec 페이지 복원
params:
  bytes: {default: 52428800}
  port:  {default: 18080}
  phase_gap_ms: {default: 400}   # phase별 정지 창(호환성 스윕용); 시간 측정 시 0
phases: [init, base_mem, f_mmap_exec_jit, ready, served_first, steady]
metrics: []
resident:
  from_param: bytes
  bytes_per_unit: 1
  overhead_mib: 10
