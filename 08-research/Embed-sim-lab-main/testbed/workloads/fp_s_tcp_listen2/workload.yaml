name: fp_s_tcp_listen2
# AUTO-GENERATED failprobe workload — single-feature probe: 보조 TCP listen 소켓 (관리 포트)
# features: tcp_listen2
# expected risks: tcp_listen2:low
params:
  bytes: {default: 52428800}
  port:  {default: 18080}
  phase_gap_ms: {default: 400}   # phase별 정지 창(호환성 스윕용); 시간 측정 시 0
phases: [init, base_mem, f_tcp_listen2, ready, served_first, steady]
metrics: []
resident:
  from_param: bytes
  bytes_per_unit: 1
  overhead_mib: 8
