name: fp_s_child
# AUTO-GENERATED failprobe workload — single-feature probe: 자식 프로세스 1 (헬퍼/플러그인 프로세스)
# features: child
# expected risks: child:low — 프로세스 트리 dump
params:
  bytes: {default: 52428800}
  port:  {default: 18080}
  phase_gap_ms: {default: 400}   # phase별 정지 창(호환성 스윕용); 시간 측정 시 0
phases: [init, base_mem, f_child, ready, served_first, steady]
metrics: []
resident:
  from_param: bytes
  bytes_per_unit: 1
  overhead_mib: 8
